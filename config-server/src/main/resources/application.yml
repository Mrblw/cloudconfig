server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Mrblw/cloud-config
          search-paths: conf
          username:
          password:
      label: master


